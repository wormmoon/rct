import React from 'react';

export default class PlaceName extends React.Component {
  render () {
    return (
      <span className="city">Brighton, </span>
    );
  }
}

// why is this not displaying on the page??
